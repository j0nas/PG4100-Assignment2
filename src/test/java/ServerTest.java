import main.Client;
import org.junit.Test;

import static org.mockito.Mockito.mock;

public class ServerTest {
    @Test
    public void testServerServesQuestions() {
        Client mockedClient = mock(Client.class);
    }
}
